// js/matching.js — Pure filtering logic, no DOM code.
// Built out Day 5 per ARCHITECTURE.md's matching data flow.
// Will export a filterIdeas(userInputs, ideas) function.

export function filterIdeas(userInputs, ideas) {
  // Placeholder — real filtering logic (difficulty, stack overlap, time fit)
  // gets implemented Day 5.
  return [];
}
