# Copy this file to .env (same folder) and fill in your real key.
# .env is already excluded by .gitignore — it must NEVER be committed.
# This .env.example file, with no real key in it, IS safe to commit.

# Get a free key (no credit card) at https://aistudio.google.com
GEMINI_API_KEY=your-gemini-key-here
